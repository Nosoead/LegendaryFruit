public enum AttributeType
{
    Burn,
    SlowDown,
    KnockBack,
    Poison,
    Blindness,
    Normal
}

enum StatType
{
    MaxHealth,
    MoveSpeed,
    Defense,
    AttackSpeed,
    AttackPower,
    Heal
}

public enum GradeType
{
    Normal,
    Rare,
    Epic,
    Legendary
}

public enum StageType
{
    Lobby,
    Normal,
    Shelter
}

public enum PoolType
{
    PooledReward,
    PooledMonster,
    PooledSound,
    PooledEffect,
    Count
}